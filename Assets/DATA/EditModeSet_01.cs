using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EditModeSet_01 : MonoBehaviour
{/*
    public DefaultSet_01 MainCanvas_Pre; // contains Default SetUP
    public List<GameObject> editMode;


    private void Start()
    {
        editMode = new List<GameObject>();

        if (editMode.Count > 0)
        {
            foreach (GameObject editM in editMode)
            {
                Destroy(editM.gameObject);
            }
            editMode.Clear();
        }

        DefaultSet_01 display = Instantiate(MainCanvas_Pre); // ? not sure what yet
        //display.SetActive(true);

        display.SetDefault(editMode);

    }
    */
}
